import type React from "react"
import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "GentleMale - Premium Men's Grooming Salon",
  description: "Redefining men's grooming with luxury services, premium products, and exceptional customer experience.",
  keywords: "men's salon, grooming, haircut, beard, luxury, premium",
    generator: 'v0.dev'
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
